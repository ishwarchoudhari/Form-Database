Django 26
This project is made for : 
Registration inputs with storing data in local database creating models
 and rendering all the registered users in next page after submitting the form (Django-admin )